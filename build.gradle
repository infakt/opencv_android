buildscript {
    repositories {
        jcenter()
        mavenCentral()
    }
    dependencies {
//        classpath "com.android.tools.build:gradle:${project.ANDROID_BUILD_TOOLS_GRADLE_VERSION}"
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
